language: python
sudo: false
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
install:
  - pip install -e .
  - pip install -q Django==$DJANGO_VERSION
  - pip install -rrequirements-dev.txt
  - pip install codecov
script:
  - make coverage
after_success:
  - codecov